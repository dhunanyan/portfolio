import { About, Contact, Experience, Hero, Overlay } from '@components';

const Page = () => (
  <main>
    <Hero />
    <Overlay />
    <About />
    <Experience />
    <Contact />
  </main>
);

export default Page;
