import { About, Contact, Experience, Hero, Overlay, Footer } from '@components';

const Page = () => (
  <main>
    <Hero />

    <Overlay />
    <About />
    <Experience />
    <Contact />
    <Footer />
  </main>
);

export default Page;
